#include <iostream>
#include <string>
using namespace std;

int main() {
    string s;
    cin >> s;               // 공백 없는 문자열 입력
    cout << s.size();       // 또는 s.length()
    return 0;
}