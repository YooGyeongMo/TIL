import UIKit

var welcome: String = "Welcome SwiftUI"

print("Welcome SwiftWorld")

// 텍스트 보려면 option + M

/*:
# 정말 바쁩니다.
 오늘 하루는 어땠나요? 너무 힘듭니다. ㅋㅋ.
 * 이것은 퀵룩입니다.
 * 코드와 함께 in line에 결과를 위치시킵니다.
 */


var num: Int = 10

for i in 0..<20 {
    let y = i * num
    num -= 1
    print(y)
}


/*:
 * 플레이그라운드는 스위프트 코드를 입력하고 그 결과를 동적으로 볼 수 있는 환경을 제공.
 * 플레이그라운드는 Xcode 프로젝트를 생성하고 코드를 반복적으로 편집, 컴파일 및 실행할 필요 없이 iOS SDK에 포함된 많은 클래스 및 API를 실험하고 스위프트 프로그래밍 언어를 학습할 수 있게 해주는 최고의 환경
 */

