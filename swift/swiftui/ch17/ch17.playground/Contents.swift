/*:
 ## Xcode Tip
 
 * Canvas 안나오면 Editor -> Canvas
 * CMD 누르고 미니맵에 가면 해당 코드로 바로갈 수 있음.
 * 프리뷰 툴바에서 재생 모양을 선택시에,라이브모드로 변경
 -> 프리뷰에서 디바이스나 시뮬레이터처럼 앱과 상호작용할 수 있다.
 * 선택 가능 모드를 사용시에, 캔버스 뷰에서 항목을 선택하여 속성을 편집하고 컴포넌트를 추가 및 제거할 수 있음.
 * Variants 버튼 사용시에 다양한 동적 텍스트 크기, 색 구성표 및 장치 방향을 뷰를 미리 볼 수 있음.
 * Modifier를 이용하여 뷰들을 수정하기 로직을 추가하고 상태와 다른 데이터 객체를 바인딩하기 등 포함된다.
 
 */


